## Conflict Detection Report

Ingest set: SPEC.md (SPEC, precedence 0) / PRD.md (PRD, precedence 1) / planning.md (DOC, precedence 2). MODE=new. Precedence order applied: SPEC > PRD > DOC.

### BLOCKERS (0)

No blockers. No ADR-classified documents were ingested, so no LOCKED decision exists and no LOCKED-vs-LOCKED contradiction is possible in this set. No existing `.planning/` PROJECT.md, REQUIREMENTS.md, ROADMAP.md, or CONTEXT.md exists to contradict. The cross-reference graph is acyclic. No UNKNOWN or low-confidence classifications were produced.

### WARNINGS (9)

[WARNING] Graph and relational store: "use existing stack" vs mandated Neo4j + PostgreSQL
  Found: SPEC.md §31 states "Use the existing stack if already established. Otherwise prefer:" before listing Neo4j and PostgreSQL, and closes with "Do not introduce technologies without a concrete reason."
  Found: PRD.md §1.1 and §4.2 FR-KNG-01 mandate "Neo4j property graph modeling"; PRD.md §4.4 FR-ANA-01 mandates "Neo4j GDS algorithms"; PRD.md §4.1 FR-ING-03 mandates custody events "stored in PostgreSQL"; planning.md §2.1 mandates "Neo4j 5.x (Cypher + GDS Library)" and "PostgreSQL 16 + pgvector" unconditionally, with no existing-stack escape clause.
  Found: ../codebase/STACK.md and ../codebase/ARCHITECTURE.md report the established stack is an in-process `networkx.MultiDiGraph` singleton (`backend/app/services/graph_engine.py:290`) rebuilt from SQLite at startup, with SQLite via SQLAlchemy as the relational store. No Neo4j dependency, driver, or Cypher exists anywhere in the repo.
  Impact: This is the largest scope fork in the ingest. Reading SPEC §31 literally, the existing NetworkX + SQLite stack is "already established" and must be reused, which invalidates every Neo4j/Cypher/GDS/pgvector acceptance criterion in PRD.md and planning.md. Reading PRD/planning literally requires a full datastore migration that touches the graph engine, all graph API routes, entity resolution, analytics, and the AI retrieval layer. Routing a roadmap without resolving this will produce phases that contradict each other. Note also that the in-memory graph is non-durable — API-created entities are lost on restart — so "keep the existing stack" is not a no-op; it still requires persistence work.
  → Decide one of: (a) migrate to the documented stack (Neo4j 5.x + PostgreSQL 16 + pgvector) and keep PRD/planning acceptance criteria intact; or (b) keep NetworkX + SQLite per SPEC §31's existing-stack clause, add graph durability, and amend PRD.md §1.1/§4.2/§4.4 and planning.md §2.1 to match. Record the choice as an ADR before roadmapping.

[WARNING] Copilot is a hardcoded question router, which SPEC.md explicitly forbids
  Found: SPEC.md §4 forbids `if question == "..."` routing and "a finite question dictionary" presented as AI, and requires a Dynamic Investigation Query Planner. SPEC.md §43 states "Never hardcode fake AI answers merely to make the UI appear functional." SPEC.md §44 makes "Investigators can ask arbitrary reasonable investigation questions rather than selecting from a fixed question set" a Definition-of-Done item.
  Found: ../codebase/CONCERNS.md reports `UniversalAIInvestigator.investigate()` (`backend/app/services/ai_investigator.py:21-133`) matches hardcoded substrings ("rahul", "vikram", "golden falcon", "250") and returns pre-written answer/citation payloads via `_build_vikram_swiss_transfer_response`, `_build_vikram_location_response`, `_build_cross_case_response`. Unmatched queries fall through to `_call_live_gemini` (`ai_investigator.py:174-224`), which has no grounding, no tool use, and no citation enforcement, and attaches a fabricated `confidence_score: 0.90` and a fabricated citation object.
  Impact: The signature feature of the product (SPEC §35 item 1, MVP-14) is not partially built — it is built as the exact anti-pattern the spec prohibits, and the fallback path actively fabricates citations, violating SPEC §26 ("Never fabricate") and PRD.md §5.2 rule 3. Treating this as "partially done" during roadmapping will badly underestimate the work. It should be scoped as new construction. There is also a demo risk: removing the scripted branches before the real planner works will break the currently working demo queries.
  → Scope the Copilot as unbuilt. Decide whether the scripted branches stay temporarily behind an explicit `DEMO_MODE` flag while the dynamic planner is built, or are deleted immediately. Either way the fabricated `confidence_score`/citation on the Gemini fallback path must be removed before anything else ships.

[WARNING] Competing acceptance variants for evidence ledger integrity (REQ-ledger-integrity-v1 vs v2)
  Found: SPEC.md §22 states "For an MVP, a local demonstrable permissioned-ledger implementation is sufficient", listing Hyperledger Fabric/Besu "or another appropriate permissioned ledger" as possibilities only.
  Found: PRD.md §7.2 FR-EVD-04 requires anchoring to a "smart contract"; planning.md §8.1 requires commitment to "a permissioned ledger smart contract (`EvidenceLedger.sol` on EVM or Hyperledger chaincode)"; planning.md §12.1 requires a `spemass-blockchain-mock` Ganache/Hardhat EVM node on port 8545.
  Found: ../codebase/CONCERNS.md reports the current implementation has no ledger of any kind — `create_evidence_record()` (`backend/app/services/evidence.py:26-40`) mints `tx_hash` from `uuid.uuid4()` with an incrementing fake block number into a SQLite `BlockchainRecord` table, and `contract_address` is the literal string `"0x71C2B890a8813C124231EVID_LEDGER_MOCK"`.
  Impact: Synthesis cannot pick a variant without losing intent. Beyond the technology fork, the current implementation satisfies neither variant: verification compares a SQLite hash against a record in the same SQLite database, so anyone with DB write access can edit both together. That directly undercuts the evidentiary-integrity value proposition asserted in PRD.md §1.2 and SPEC.md §35 item 5, and MVP-20 / Phase 6 DoD ("altering 1 byte fails hash verification and alerts") cannot be honestly claimed.
  → Choose REQ-ledger-integrity-v1 (any local demonstrable permissioned ledger, SPEC scope) or REQ-ledger-integrity-v2 (EVM smart contract + Ganache/Hardhat container, PRD/planning scope). Both variants are preserved in `intel/requirements.md`. Whichever is chosen, the attestation store must be independent of the SQLite database it attests, or the feature must be explicitly relabelled "simulated ledger — demonstration only" everywhere it surfaces.

[WARNING] No vector/semantic retrieval exists, and GraphRAG depends on it
  Found: SPEC.md §26 requires a retrieval chain including "Vector retrieval"; SPEC.md §31 permits "pgvector or Qdrant". PRD.md §4.1 FR-ING-02 requires OCR output to generate "chunk embeddings in `pgvector`"; PRD.md §5.1 describes the agent operating over "vector search (`pgvector`)"; PRD.md UAT-2 requires chunks embedded in a vector index. planning.md §5.3 specifies a full `document_chunks` table with `embedding vector(384)` and an ivfflat cosine index, and §3.2 step 6 specifies Sentence-Transformers embeddings.
  Found: ../codebase/CONCERNS.md reports pgvector semantic search is "Not implemented anywhere — no embedding model, no vector index, no `pgvector` dependency" in `backend/requirements.txt`, and no `document_chunks` table exists in `backend/app/models/entities.py`.
  Impact: MVP-15 (GraphRAG), MVP-16 (evidence-grounded responses), UAT-2 and UAT-5 all transitively depend on document retrieval that does not exist. Without a decision on the retrieval store, GraphRAG cannot be phased. This is also coupled to the first warning: choosing SQLite as the relational store forecloses pgvector and forces a separate vector store (Qdrant, or SQLite FTS/embedding table).
  → Decide the retrieval substrate: pgvector (requires PostgreSQL, coupling to warning 1), Qdrant (SPEC-permitted, independent of the relational choice), or a documented reduced-scope keyword/BM25 retrieval for the SIH prototype with the vector requirement deferred. Record the choice before the GraphRAG phase is planned.

[WARNING] RBAC and case isolation are declared everywhere but enforced nowhere
  Found: SPEC.md §32 requires "authentication, RBAC, authorization checks, case-level access control where practical"; SPEC.md §5 places an Authorization Check before query planning; SPEC.md §39 requires security tests for unauthorized case access. PRD.md §8 requires four roles (System Admin, Lead Investigator, Analyst, Viewer) and "Users can only query cases explicitly assigned to them"; PRD.md UAT-10 requires a cross-case query by an unauthorized user to be "strictly rejected" with a security audit log. planning.md RSK-04 requires "Row-level and Cypher-level case tenancy filters; strict RBAC checks injected into all LLM tool calls."
  Found: ../codebase/CONCERNS.md reports no RBAC middleware or dependency exists, no route in `backend/app/api/*.py` carries a `Depends(get_current_user)` guard, `create_access_token` emits no role claim, nothing validates the issued JWT on any subsequent request, and `GET /auth/me` (`backend/app/api/auth.py:48-57`) returns `db.query(User).first()` regardless of the caller.
  Impact: UAT-10 cannot pass and cannot be made to pass by a small change — it requires role claims in the token, a verification dependency on every router, and case-scoping parameters threaded through every graph query method (`resolve_entity`, `get_k_hop_neighborhood`, etc., which currently accept no caller context). PRD.md §3 tiers RBAC as [MVP], not [MVP-Lite], so it is not currently deferrable by the documents.
  → Decide the security scope for the SIH prototype: full [MVP] RBAC with case tenancy per PRD.md §8 and UAT-10, or an explicitly documented reduced scope (authentication enforced, single-role, case isolation deferred) with PRD.md §8 and UAT-10 amended accordingly. Do not leave the documents claiming enforcement that does not exist.

[WARNING] Performance NFRs are unattainable on the current analytics path
  Found: PRD.md §8 requires a 2-hop traversal over 50,000 nodes in <= 350ms, tri-view sync <= 200ms, and first AI token <= 2.0s; PRD.md §4.3 FR-VIS-01 requires >= 1,000 nodes rendered smoothly; PRD.md §12.1 lists "Sub-200ms latency on Tri-View workspace synchronization" as a release gate.
  Found: ../codebase/CONCERNS.md reports `_build_subgraph_response()` calls `compute_graph_analytics()` — including betweenness centrality, O(V·E) or worse — from scratch over the entire graph on every subgraph request, including `get_k_hop_neighborhood` and `get_shortest_path` (`backend/app/services/graph_engine.py:247-250`), with no caching. Separately `_call_live_gemini()` issues a blocking synchronous `requests.post(...)` with an 8s timeout inside an async FastAPI app, stalling the event loop.
  Impact: The 350ms/50k-node target is not reachable against uncached full-graph betweenness centrality regardless of datastore choice, and the 2.0s AI token target is not reachable through a blocking 8s-timeout call. Carrying these NFRs into a roadmap unchanged will produce a phase that is guaranteed to fail its own DoD.
  → Either commit to the optimization work (per-case analytics caching with mutation invalidation, lazy centrality computation, `httpx.AsyncClient` or `run_in_threadpool` for the LLM call, and k-hop server-side limits per planning.md RSK-03) or restate the NFR targets at prototype-realistic values against the actual synthetic dataset size. Do not carry both the current code path and the current targets forward.

[WARNING] PRD.md release gate is pre-marked complete but describes an unbuilt system
  Found: PRD.md §12.1 marks every MVP Release Gate item as checked `[x]`, including "All 25 MVP capabilities (MVP-01 to MVP-25) fully implemented and operational", "100% pass rate on all 10 UAT scenarios", "Universal AI Investigator citation validation passes all test suites (zero ungrounded claims)", and "Complete Docker Compose orchestration for local 1-click startup".
  Found: ../codebase/STACK.md reports no Dockerfile, docker-compose, or CI/CD config anywhere in the repo, and no test framework configured. ../codebase/CONCERNS.md reports the Copilot returns canned responses with fabricated citations and that no route enforces authentication, so UAT-5, UAT-6, UAT-7, UAT-9 and UAT-10 cannot pass.
  Impact: A pre-checked gate is a status assertion that contradicts observable state. If the roadmapper treats PRD §12.1 as descriptive it will mark the entire MVP as already delivered and produce an empty or near-empty roadmap.
  → Treat PRD.md §12.1 as an aspirational target checklist, not a status report, and correct the checkboxes in PRD.md so the document does not assert false completion. All items should be unchecked until verified against SPEC.md §44.

[WARNING] Object storage and the hash-before-write ordering requirement
  Found: PRD.md §4.1 FR-ING-03 requires "Hash computed before persistent write to MinIO"; PRD.md UAT-9 specifies the tamper test as altering one byte "of stored PDF in MinIO"; PRD.md §8 requires AES-256 at rest for "Database & MinIO"; planning.md §2.1, §3.2 step 2, §12.1 all specify MinIO/S3; planning.md §5.2 stores a `storage_uri` of the form `s3://spemass-evidence/...`. SPEC.md §31 lists "MinIO / S3-compatible" object storage but under the same "use the existing stack if already established" condition.
  Found: ../codebase/CONCERNS.md and ../codebase/STACK.md report evidence blobs are written to the local filesystem `backend/storage/` via `aiofiles`, with no object-storage client present.
  Impact: FR-ING-03's ordering requirement (hash before persistent write) and UAT-9's execution steps are written against an object store that does not exist. The at-rest encryption NFR has no implementation target. Deferring this silently leaves the evidence chain's storage layer unspecified.
  → Decide whether to add MinIO (aligning with PRD/planning and the Docker Compose topology) or to formally accept local filesystem storage under SPEC §31's existing-stack clause and rewrite FR-ING-03, UAT-9, PRD §8 at-rest encryption, and the planning.md `storage_uri` scheme to match.

[WARNING] Competing acceptance variants for Copilot answer format (REQ-evidence-grounded-answers-v1 vs v2)
  Found: SPEC.md §20 specifies the answer shape as ANSWER / Evidence / Sources / Confidence rendered as "High / Medium / Low" / Contradictions / Limitations / Suggested next step, with clickable citations.
  Found: PRD.md §5.2 rule 3 requires every factual statement to carry an inline `[EvidenceID: SourceDocument, p. X / Row Y]` citation, and PRD.md §5.3 renders confidence as a numeric score ("Confidence: High (0.94)") over the section set Direct Answer / Confidence / Limitations / Visual Action — omitting Sources and Suggested next step, and placing citations inline rather than in a trailing list.
  Impact: The two documents prescribe different section sets and incompatible confidence representations (categorical vs numeric) for the same output. Picking one silently would lose either SPEC's suggested-next-step and sources sections or PRD's per-statement inline citation discipline and numeric scoring. Both feed the same `intel/constraints.md` Copilot response contract, so the ambiguity would propagate into the API schema.
  → Choose one variant, or unify explicitly: SPEC's section set with PRD's inline per-statement citations and a numeric confidence rendered alongside the categorical band. Both variants are preserved separately in `intel/requirements.md`. Resolve before the Copilot response contract is frozen.

### INFO (18)

[INFO] Precedence applied across the ingest set
  Note: Manifest precedence values were honoured — SPEC.md (0) > PRD.md (1) > planning.md (2). SPEC.md self-declares supersession over the other two ("supersedes PRD.md and planning.md where they conflict", line 3), which is consistent with its manifest precedence, so no override reconciliation was needed.

[INFO] No ADR-classified documents; no LOCKED decisions in scope
  Note: The ingest set contains one SPEC, one PRD, and one DOC. All three classifications carry `locked: false`. `intel/decisions.md` therefore records every decision with `status: proposed`, and nothing in this ingest can be blocked by a LOCKED-vs-LOCKED contradiction. MODE=new with no pre-existing `.planning/` PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md or CONTEXT.md, so there is no existing locked decision to contradict either.

[INFO] Cross-reference graph is acyclic
  Note: Cycle detection ran over the `cross_refs` graph: SPEC.md → PRD.md and SPEC.md → planning.md; PRD.md and planning.md declare no outbound references. Maximum traversal depth 2, well under the depth-50 abort cap. No cycles found; the full set was synthesized.

[INFO] planning.md classified DOC despite SPEC-shaped content
  Note: The classifier recorded that planning.md self-labels "Classification: Technical Specification / Engineering Architecture" and contains SPEC-like content (technology stack table, Neo4j graph schema, PostgreSQL DDL, architecture diagrams) that would otherwise indicate type SPEC. The manifest override to DOC was applied as authoritative, placing it at the lowest precedence of the set. Consequence: its unconditional stack mandates lose to SPEC.md §31's conditional stack table wherever they conflict. Its schemas and DDL were still extracted into `intel/constraints.md` as constraint content with DOC-level attribution.

[INFO] Auto-resolved: map library — Leaflet
  Note: PRD.md §1.1 and §4.3 FR-VIS-02 and planning.md §2.1/§2.2 specify MapLibre GL JS. SPEC.md §11 and §31 permit "MapLibre GL JS or Leaflet". SPEC wins on precedence and its permission is a superset, so the implemented Leaflet ^1.9.4 map (`frontend/src/components/map/InvestigatorMap.tsx`) satisfies the requirement as written. No migration is required by the documents. Note that planning.md §2.2's rationale for MapLibre (hardware-accelerated vector tiles, time-stamped trajectory animation layers) is a genuine capability argument if the temporal replay animation proves hard on Leaflet.

[INFO] Auto-resolved: frontend framework — React SPA
  Note: planning.md §2.1/§2.2/§3.1/§12.1 mandate Next.js 14 (App Router) + Shadcn UI + Zustand. SPEC.md §31 permits "React / Next.js" and §31's leading clause prefers the existing stack. SPEC wins on precedence, so the implemented Vite + React 19 SPA satisfies the requirement. The lifted-`useState` prop-drilling pattern in `frontend/src/App.tsx` also satisfies planning.md §6.1's "single unified state machine" intent, though not via Zustand.

[INFO] Auto-resolved: timeline library
  Note: PRD.md §4.3 FR-VIS-03 and planning.md §2.1 specify vis-timeline. SPEC.md §12 specifies timeline behavior without naming a library and does not list a timeline library in the §31 stack table. SPEC wins on precedence and is silent on the library, so the existing custom `InvestigatorTimeline.tsx` component is permitted, provided it meets the FR-VIS-03 range-scrubber acceptance criterion.

[INFO] Auto-resolved: OCR engine choice
  Note: PRD.md §4.1 FR-ING-02 mandates Tesseract; planning.md §2.1 mandates Tesseract + PyMuPDF + Unstructured.io. SPEC.md §25 and §31 permit "PaddleOCR or Tesseract" and "spaCy, Transformers". SPEC's permission is a superset, so either engine satisfies the documents. This resolves the engine choice only — the pipeline itself is absent (see the OCR gap entry below).

[INFO] Auto-resolved: vector store choice
  Note: PRD.md and planning.md name pgvector specifically; SPEC.md §31 permits "pgvector or Qdrant". SPEC's superset governs the technology choice. The substantive question of whether any vector retrieval exists is escalated as a WARNING above, not resolved here.

[INFO] Auto-resolved: community detection algorithm
  Note: PRD.md §4.4 FR-ANA-01 mandates "Louvain community detection" via "Neo4j GDS algorithms"; planning.md Phase 5 DoD requires "Louvain clusters colored". SPEC.md §16 requires "community detection" generically without naming an algorithm or engine. SPEC wins, so a non-GDS community detection implementation is acceptable. Note that the current `nx.connected_components` (`backend/app/services/graph_engine.py:159-164`) is basic component labeling, not community detection, so the requirement remains unmet regardless of algorithm choice.

[INFO] Auto-resolved: API route naming deviation is permitted
  Note: SPEC.md §37 lists namespaces including `/copilot/query`, `/copilot/plan`, `/copilot/actions`, `/hypotheses`, `/insights`, `/integrity`, `/chain-of-custody`, `/audit`, while PRD.md §10 specifies concrete `/api/v1/...` routes using `/ai/investigate` and nested case paths. SPEC.md §37 explicitly states "The exact API design may differ based on the existing project", which resolves the naming difference in favour of whatever the implementation establishes. The existing `/api/v1/ai/investigate` route is therefore acceptable. The namespaces with no implementation at all (`/hypotheses`, `/insights`, `/copilot/plan`, `/integrity`, `/audit`) are captured as requirements, not conflicts.

[INFO] Auto-resolved: implementation phase plan
  Note: SPEC.md §42 defines an 19-entry phase list (Phase 0 through Phase 18); planning.md §9 defines a different 10-phase, 10-sprint plan with per-phase Definitions of Done. Both are recorded in `intel/constraints.md`. SPEC.md §42 governs sequencing on precedence; planning.md's per-phase DoD statements remain useful as acceptance detail and were extracted into the corresponding requirements. Both plans agree on the key ordering constraint that UI polish and testing come last.

[INFO] Gap: OCR and document-intelligence pipeline is entirely absent
  Note: SPEC.md §25 / MVP-03, PRD.md FR-ING-02, PRD.md UAT-2 and planning.md Phase 2 all require an OCR pipeline. ../codebase/CONCERNS.md reports no OCR code and no OCR dependency (Tesseract, PyMuPDF, PaddleOCR, Unstructured.io, spaCy) in `backend/requirements.txt`. This is unbuilt work, not a document disagreement — captured as REQ-document-ocr-extraction. No decision needed; scope it as a phase.

[INFO] Gap: evidence verification can report VERIFIED without recomputing a hash
  Note: ../codebase/CONCERNS.md reports `verify_evidence_integrity()` (`backend/app/services/evidence.py:88-97`) falls back to comparing `stored_hash` with itself when both `document.extracted_text` and `current_content_bytes` are absent, returning "Evidence integrity confirmed" without any recomputation. This contradicts SPEC.md §21 and PRD.md FR-EVD-02, both of which require a recomputation-based comparison. No decision is needed — the behavior is unambiguously wrong and belongs in the evidence-engine phase alongside REQ-tamper-detection.

[INFO] Gap: demo tamper backdoor lives in the production evidence router
  Note: ../codebase/CONCERNS.md reports a module-level `tampered_mock_cache: Dict[str, bytes]` in `backend/app/api/evidence.py:12` with routes at lines 31, 50 and 68-69 that write, read and delete it, exposing `simulate-tamper` and `restore-clean` endpoints. SPEC.md §21 requires the tamper-detection demonstration, so the capability is wanted; SPEC.md §32 requires prototype-level security, so the endpoints should be gated. Recommended handling during the evidence phase: gate behind a `DEMO_MODE` env flag or move to a separate demo-only router that is not mounted by default.

[INFO] Gap: no test framework, no container build, no CI
  Note: SPEC.md §39 requires tests across Copilot, Graph, Map, Evidence, OSINT and Security; PRD.md §11 requires ten UAT scenarios at a 100% pass rate; planning.md Phase 10 requires end-to-end integration tests and Docker Compose deployment. ../codebase/STACK.md reports `pytest` absent from `backend/requirements.txt`, no Jest/Vitest config in `frontend/`, `backend/test_mvp.py` being a standalone `requests` script rather than an integrated suite, and no Dockerfile, docker-compose or CI/CD config anywhere. Captured as REQ-test-suite, REQ-uat-suite and REQ-deployment-packaging.

[INFO] Gap: undeclared dependency breaks a clean install
  Note: ../codebase/STACK.md reports `backend/app/core/config.py` imports `from pydantic_settings import BaseSettings` while `pydantic-settings` is absent from `backend/requirements.txt`, so a fresh `pip install -r requirements.txt` will fail to start the app. `backend/requirements.txt` also uses unpinned `>=` ranges with no lockfile. Not a document conflict; a foundation-phase fix. Also worth verifying `backend/.env` is git-ignored before any commit.

[INFO] Gap: password hashing and JWT validation weaknesses
  Note: ../codebase/CONCERNS.md reports `verify_password` (`backend/app/core/security.py:12-14`) returns True when the plaintext equals the stored hash; `SECRET_KEY` has a committed default (`backend/app/core/config.py:9`) and the password salt is a fixed literal shared across all users (`backend/app/core/security.py:9`); `passlib[bcrypt]` is a declared but unused dependency, with raw salted SHA-256 used instead; and CORS is `allow_origins=["*"]` with `allow_credentials=True` (`backend/app/main.py:17-24`). These contradict SPEC.md §32's secure-handling requirements. No decision needed — scope into the security and audit phase alongside REQ-rbac-case-isolation and REQ-secure-handling.
